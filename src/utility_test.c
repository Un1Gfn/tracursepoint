// reset; gcc -std=c99 -g -O0 -Wall -Winline utility_test.c utility.c

#include <stdio.h>
#include "utility.h"

int main(){

  // printf("%d\n",get_sensitivity());
  // printf("%d\n",get_speed());

  // set_sensitivity(150);
  // set_sensitivity(149);
  // set_sensitivity(999);
  // set_sensitivity(-1);
  // set_sensitivity(150);
  
  return 0;
}